{
  "name": "mcp-servers",
  "version": "1.0.0",
  "private": true,
  "description": "Collection of 10 production-ready Model Context Protocol (MCP) servers built in TypeScript",
  "workspaces": [
    "servers/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
